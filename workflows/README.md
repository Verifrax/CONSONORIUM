# Runtime Workflows

This directory holds runtime workflow definitions and wrappers for audit, reconcile, repair, quarantine, projection, and epoch publication flows.

The runtime may also materialize a deterministic mechanical-repair application report at
`reports/generated/sovereign-mechanical-repair-application.json`.
