name: Symphony Repair

on:
  workflow_dispatch:

jobs:
  placeholder-structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          set -euo pipefail
          test -d planners/repair
          test -d publishers/prs
