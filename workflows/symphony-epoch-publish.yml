name: Symphony Epoch Publish

on:
  workflow_dispatch:

jobs:
  placeholder-structure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          set -euo pipefail
          test -d publishers/epochs
          test -d app/github-app
